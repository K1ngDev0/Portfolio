<VirtualHost *:80> 
    DocumentRoot "C:/laragon/www/memeSlide"
    ServerName memeSlide.test
    ServerAlias *.memeSlide.test
    <Directory "C:/laragon/www/memeSlide">
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>

# If you want to use SSL, enable it by going to Menu > Apache > SSL > Enabled
