<VirtualHost *:80> 
    DocumentRoot "C:/laragon/www/takenlijst"
    ServerName takenlijst.test
    ServerAlias *.takenlijst.test
    <Directory "C:/laragon/www/takenlijst">
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>

# If you want to use SSL, enable it by going to Menu > Apache > SSL > Enabled
